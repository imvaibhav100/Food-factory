# Flowchart Example (SVG)

The following image reproduces the example flowchart you pasted.

![Flowchart Example](assets/examples/flowchart_example.svg)

- Start → Input a → `c = 1`
- Decision: `is c ≤ 5` → Yes → Print a → `c = c + 1` → loop
- No → Stop
